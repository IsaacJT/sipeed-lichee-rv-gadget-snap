#cloud-config
debug:
  verbose: true
users:
  - name: ubuntu
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
chpasswd:
  list: |
    ubuntu:ubuntu
  expire: false
datasource_list: [ NoCloud ]
network: {config: disabled}
